fastapi>=0.110
starlette>=0.46
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.9
pillow>=10.4
numpy>=1.26

# Only needed once you export real models from the notebook (CPU build is fine — no GPU needed for inference).
# Install with: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.3
